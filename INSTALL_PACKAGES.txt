#linux system

# require 
bedtools
#which can be found in the following link:
https://bedtools.readthedocs.io/en/latest/content/installation.html


#required R package:
#data.table

#python packages:

#packages default to python: (should already be installed)
#os
#glob
#getopt
#sys
#subprocess

#package required installation:
#numpy
#multiprocessing
#scipy

pip install numpy --user
pip install scipy --user
pip install multiprocess --user

R
install.packages('data.table')
